Also write 5-8 relevant hashtags for this same video (language, topic, and general language-learning/shadowing audience tags). Each hashtag must start with "#", no spaces, lowercase.

Output ONLY a single JSON object with exactly this shape — no markdown code fence, no commentary before or after:

{
  "title": "<title, use \n for a line break if 2 lines>",
  "description": "<description>",
  "hashtags": ["#tag1", "#tag2", "..."]
}
