# LLM providers — set only the one(s) referenced by config.yaml / --llm
GEMINI_API_KEY=
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
DEEPSEEK_API_KEY=

# TTS providers — edge-tts (default) needs no key
ELEVENLABS_API_KEY=

# Upload credentials (optional — pipeline generates files locally if unset)
TIKTOK_API_KEY=
YOUTUBE_API_KEY=
FACEBOOK_API_KEY=
